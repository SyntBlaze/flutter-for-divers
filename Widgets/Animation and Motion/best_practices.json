[
  "Use `AnimatedAlign` for destination-driven alignment changes with deliberate constraints",
  "Keep `AnimatedContainer` focused on a coherent set of changing properties",
  "Verify focus, semantics, and size behavior on both sides of an `AnimatedCrossFade`",
  "Let inheriting text use `AnimatedDefaultTextStyle` instead of duplicating style tweens",
  "Keep `AnimatedList` data mutations and visual state calls in one consistent operation",
  "Capture removed data before invoking `AnimatedListState.removeItem`",
  "Give an `AnimatedModalBarrier` an accessible label and intentional dismissal policy",
  "Pair fully transparent `AnimatedOpacity` content with appropriate pointer and semantics handling",
  "Avoid deeply nested relayout work beneath frequently changing `AnimatedPadding`",
  "Use `AnimatedPhysicalModel` clipping and shadow effects only where their rendering cost is justified",
  "Reserve `AnimatedPositioned` for geometry that truly must participate in `Stack` layout",
  "Set an intentional pivot and test hit targets when using `AnimatedRotation`",
  "Avoid scaling controls so far that their visible and interactive bounds become misleading",
  "Keep constraints predictable and test overflow while `AnimatedSize` is between endpoints",
  "Use `AnimatedSlide` for visual motion that should not rearrange neighboring widgets",
  "Assign semantic keys and customize incoming and outgoing transitions in `AnimatedSwitcher`",
  "Scope `AnimatedTheme` to the subtree that needs the theme transition",
  "Use `AnimatedWidget` for a reusable component with a stable external `Listenable`",
  "Hoist invariant subtrees into the `AnimatedBuilder.child` parameter",
  "Interpolate compatible decorations and avoid unnecessary foreground painting in `DecoratedBoxTransition`",
  "Drive `DefaultTextStyleTransition` with a text-style tween that has complete endpoints",
  "Combine `FadeTransition` with interaction controls when invisible content must be inactive",
  "Keep `Hero` tags unique per route and preserve visual continuity between endpoints",
  "Use `PositionedTransition` only as a direct child of the intended `Stack`",
  "Express `RotationTransition` values in turns and select the alignment explicitly",
  "Reuse a controller with `ScaleTransition` only when the motions belong to one timeline",
  "Choose the reveal axis and alignment deliberately for `SizeTransition`",
  "Use directional offsets for `SlideTransition` when motion should mirror in right-to-left layouts"
]
